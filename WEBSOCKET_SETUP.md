# WebSocket Configuration for Railway

# Add this environment variable to Railway for WebSocket URL override:

WEBSOCKET_URL=wss://your-railway-app.up.railway.app:4001

# Or if Railway uses different WebSocket endpoint:
WEBSOCKET_URL=wss://your-railway-app-websocket.up.railway.app

# This will override the automatic WebSocket URL detection